Áno, na https://www.saucedemo.com/ sa prihlásenie neukladá v cookies ani v localStorage/sessionStorage – stránka používa čistý „in-memory“ login. To znamená:

SELECT * FROM  ORDER BY timestamp DESC LIMIT 10;

allure generate ./allure-results -o ./allure-report

allure open ./allure-report